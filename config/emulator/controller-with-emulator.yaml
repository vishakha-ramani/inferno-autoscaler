apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
        - name: manager
          env:
            - name: EMULATOR_MODE # Enable emulator mode, consumes fake gpu resources on kind cluster
              value: "true"